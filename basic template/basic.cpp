#include <bits/stdc++.h>
using namespace std;

// Fast I/O
#define fast_io ios::sync_with_stdio(false); cin.tie(NULL);

// Type aliases
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pii;

// Useful constants
const int INF = 1e9;
const ll MOD = 1e9 + 7;

// For multiple test cases
void solve() {
    // Write code for one test case here
}

int main() {
    fast_io

    int t = 1;
    cin >> t;          // Comment this line if only one test case
    while (t--) {
        solve();
    }

    return 0;
}
